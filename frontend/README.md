# How to develop
```
$ cp src/auth0/config.sample.json src/auth0/config.json
// fill in values

$ cp src/appsync/config.sample.json src/appsync/config.json
// fill in values

// Generate code
$ APP_ID=xxx yarn codegen

$ yarn start
```
